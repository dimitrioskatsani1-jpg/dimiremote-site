# DimiRemote

Professional one-page website for DimiRemote, built as a lightweight static site for GitHub Pages.

## Local preview

Serve the repository root with any static web server, then open `index.html`.

## Branding assets

The current header uses a text-based DimiRemote wordmark. A future logo can be added under `assets/`.
